#ifndef COMMITMENT_HPP
#define COMMITMENT_HPP

#include <iostream>
#include <cstring>
using namespace std;

// A structure to describe a commitment
struct Commitment {
    string description;
    int hour;
    int minute;
};

#endif
